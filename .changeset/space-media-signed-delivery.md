---
'@atproto/bsky': minor
---

Serve private space media through expiring signed URLs minted at hydration: deterministic window-aligned presigns for image blobs, HMAC-signed playback URLs for video, projections keep blob-bearing embeds, and `mediaBlobsReachable` turns on when delivery is configured. With no delivery configuration, space media stays withheld exactly as before.
